[package]
name = "rish-registry"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
repository = "https://github.com/ZSeven-W/rish"
description = "Portable OCI Distribution registry primitives for rish"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
thiserror = "2.0"

[dev-dependencies]
serde_json = "1.0"
