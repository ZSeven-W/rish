[package]
name = "rish-guest-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
repository = "https://github.com/ZSeven-W/rish"
description = "Versioned host-to-guest protocol for the rish Linux VM runtime"

[dependencies]
base64 = "0.22"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
